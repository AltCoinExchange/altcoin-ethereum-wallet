import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class WETHTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class WETHTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
