import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class StorjTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class StorjTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
