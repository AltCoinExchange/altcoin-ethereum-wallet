import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class AragonTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class AragonTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
