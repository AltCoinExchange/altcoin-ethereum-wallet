import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class CivicTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class CivicTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
