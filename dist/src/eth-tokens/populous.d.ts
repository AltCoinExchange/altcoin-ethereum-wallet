import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class PopulousTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class PopulousTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
