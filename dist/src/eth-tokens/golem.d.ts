import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class GolemTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class GolemTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
