import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class OmiseGoTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class OmiseGoTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
