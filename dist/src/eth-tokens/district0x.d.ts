import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class District0xTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class District0xTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
