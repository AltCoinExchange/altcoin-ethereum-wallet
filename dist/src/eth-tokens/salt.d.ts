import { EthEngine } from "../eth/eth-engine";
export declare class SaltTokenTestnet {
    constructor(ethEngine: EthEngine);
}
export declare class SaltTokenMainnet {
    constructor(ethEngine: EthEngine);
}
