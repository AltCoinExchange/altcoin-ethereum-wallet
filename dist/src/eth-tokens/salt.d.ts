import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class SaltTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class SaltTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
