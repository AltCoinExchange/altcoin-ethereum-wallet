import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class LunyrTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class LunyrTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
