import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class DentTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class DentTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
