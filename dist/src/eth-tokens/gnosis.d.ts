import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class GnosisTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class GnosisTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
