import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class IconomiTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class IconomiTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
