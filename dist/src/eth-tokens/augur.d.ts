import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class AugurTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class AugurTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
