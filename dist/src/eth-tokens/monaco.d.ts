import { EthEngine } from "../eth/eth-engine";
import { DEX } from "../eth/tokens/dex";
export declare class MonacoTokenTestnet extends DEX {
    constructor(ethEngine: EthEngine);
}
export declare class MonacoTokenMainnet extends DEX {
    constructor(ethEngine: EthEngine);
}
