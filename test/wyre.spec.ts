import "jest";
import { WyreVerify } from '../src/wyre';


describe("WyreVerify", () => {
  jasmine.DEFAULT_TIMEOUT_INTERVAL = 1000000;
  // it("Should be pass sanity", () => {
  //   expect(typeof 1).toBe("number");
  // });

  // it("Should check user status", async () => {

  //   const wyre = new WyreVerify();
  //   wyre.checkUserStatus('0xd294DeE5901703f17A24B3170cCAf08C8109207d').then(status => { 
  //     expect(status).toBeGreaterThan(0);
  //   });
    
  // });
  
});
